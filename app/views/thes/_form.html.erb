<%= form_for(@the) do |f| %>
  <% if @the.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@the.errors.count, "error") %> prohibited this the from being saved:</h2>

      <ul>
      <% @the.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="control-group">
      <%= f.label :name, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :name, :class => 'form-control' %>
      </div>
    </div>
    <!--<script>-->
//        $(function(){
            <!--<% if @the.department.present? %>-->
//            $('#the_deparment').val('<%= @user.department_id  %>');
            <!--<% end%>-->
//        })
    <!--</script>-->

    <!--<div class="control-group">-->
      <!--<%= f.label "角色", :class => 'control-label' %>-->
      <!--<div class="controls">-->
        <!--<select name="the[deparment]" id="the_deparment" class="form-control">-->
          <!--<option value>-&#45;&#45;请选择专业-&#45;&#45;</option>-->
          <!--<%= options_for_select(Department.all.map{|j| [j.name,j.id]}) %>-->
        <!--</select>-->
      <!--</div>-->
    <!--</div>-->
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
